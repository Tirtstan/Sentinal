using UnityEngine;

namespace Sentinal
{
    /// <summary>
    /// Attribute to mark a field as a view group mask selector.
    /// Used by the editor to display a mask field similar to Unity's LayerMask.
    /// </summary>
    public class ViewGroupMaskAttribute : PropertyAttribute { }
}
